# Agent Instructions

The authoritative portfolio collaboration policy is private. When the sibling
workspace is available, read these first:

- `../portfolio-site/STATUS.md`
- `../portfolio-site/COLLABORATION.md`

Then read this repository's `README.md` and inspect its literal Git state. The
relative references intentionally reveal only the sibling folder name, not its
contents or an absolute local path. After meaningful work, update the private
status and history as directed there; do not copy public site source into the
private repository.

## Non-authoritative fallback

Last synced: 2026-08-10. Use this only when the sibling workspace is unavailable;
the private `COLLABORATION.md` remains authoritative.

- Start meaningful work with no more than two sentences naming the outcome,
  why it matters, and what is out of scope. If the intent is clear, proceed.
- Communicate naturally and specifically. Make a recommendation, avoid canned
  praise and performative variety, and never force humor.
- Make the smallest coherent change and leave unrelated working code alone.
- If something fails, separate code, content, environment, and assumption
  failures; try the simplest safe repair, then rethink rather than stack fixes.
- Verify in proportion to risk. Inspect UI changes at real operating sizes;
  routine documentation changes do not require a full application build.
- Never push `main` without Jason's explicit authorization; every push deploys.
- Keep QuickBin/JGI code, data, internal artifacts, and repository material
  private unless Brian/JGI clears the specific item.
